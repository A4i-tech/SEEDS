name: Execute Workflows

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  run-backend-workflow:
    uses: ./.github/workflows/deploy.yml
    secrets: inherit

  run-coverage-summary:
    needs: run-backend-workflow
    uses: ./.github/workflows/coverage-summary.yml
    secrets: inherit
