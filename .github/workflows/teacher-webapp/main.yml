name: Teacher Web App Main

on:
  push:
    branches: [ a4i/main ]
    paths:
      - 'teacher-webapp/**'
  pull_request:
    branches: [ a4i/main ]
    paths:
      - 'teacher-webapp/**'

jobs:
  test:
    uses: ./.github/workflows/teacher-webapp/test.yml
  
  deploy-to-docker:
    needs: test
    if: github.ref == 'refs/heads/a4i/main' 
    uses: ./.github/workflows/teacher-webapp/deploy-docker.yml
    secrets: inherit

  deploy-to-render:
    needs: deploy-to-docker
    if: github.ref == 'refs/heads/a4i/main'  && (vars.DEPLOYMENT_TARGET == 'render' || vars.DEPLOYMENT_TARGET == 'both' || vars.DEPLOYMENT_TARGET == '' || vars.DEPLOYMENT_TARGET == null)
    uses: ./.github/workflows/teacher-webapp/deploy-render.yml
    secrets: inherit
    with:
      service_name: teacher-webapp

  deploy-to-azure:
    needs: deploy-to-docker
    if: github.ref == 'refs/heads/a4i/main'  && (vars.DEPLOYMENT_TARGET == 'azure' || vars.DEPLOYMENT_TARGET == 'both')
    uses: ./.github/workflows/teacher-webapp/deploy-azure.yml
    secrets: inherit
    with:
      service_name: teacher-webapp
