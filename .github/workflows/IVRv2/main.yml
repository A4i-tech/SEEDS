name: IVR v2 Main

on:
  push:
    branches: [ a4i/main , testing-pipeline-seeds]
    paths:
      - 'IVRv2/**'
  pull_request:
    branches: [ a4i/main , testing-pipeline-seeds]
    paths:
      - 'IVRv2/**'

jobs:
  test:
    uses: ./.github/workflows/IVRv2/test.yml
  
  deploy-to-docker:
    needs: test
    if: github.ref == 'refs/heads/a4i/main' 
    uses: ./.github/workflows/IVRv2/deploy-docker.yml
    secrets: inherit
    
  deploy-to-render:
    needs: deploy-to-docker
    if: github.ref == 'refs/heads/a4i/main'  && (vars.DEPLOYMENT_TARGET == 'render' || vars.DEPLOYMENT_TARGET == 'both' || vars.DEPLOYMENT_TARGET == '' || vars.DEPLOYMENT_TARGET == null)
    uses: ./.github/workflows/IVRv2/deploy-render.yml
    secrets: inherit
    with:
      service_name: ivr-v2

  deploy-to-azure:
    needs: deploy-to-docker
    if: github.ref == 'refs/heads/a4i/main'  && (vars.DEPLOYMENT_TARGET == 'azure' || vars.DEPLOYMENT_TARGET == 'both')
    uses: ./.github/workflows/IVRv2/deploy-azure.yml
    secrets: inherit
    with:
      service_name: ivr-v2
