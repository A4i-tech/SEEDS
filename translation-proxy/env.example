ORIGIN_URL=https://content-webapp-service-c8enafb4djgaanh9.southindia-01.azurewebsites.net
PORT=4000

# Platform backend base URL the SDK calls for real AI translation.
PLATFORM_API_BASE=http://localhost:8000

# Set true to fall back to the in-memory mock /translations routes
# (rollback path if the Platform integration has issues).
MOCK_TRANSLATIONS=false
