"""
Authorization guardrails for SEEDS Platform.

Sub-modules:
  tenant_scope  — assert_same_tenant() for tenant-isolation enforcement
  ownership     — assert_conference_owner() for resource ownership checks
  audit         — log_denial() for structured denial audit log
"""
